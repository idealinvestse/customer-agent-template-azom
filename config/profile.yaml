# Single-tenant customer profile (Azom defaults).
# Safety rails (never-suggest, fail-closed, kill-switch names) are NOT overridable here.
customer: azom
brand: Azom
markets: [se, no, dk]
languages: [sv, nb, da]
actors:
  viewer: jonatan
  admin: oscar
  operator: agent
# Used in customer drafts when Woo has no tracking_link.
order_status_url_template: "https://azom.se/order-status/?order_id={order_id}"
